"""Runnable reference examples for ``convilyn-edge`` (not shipped in the wheel).

A package marker so the in-repo test-suite can import example packs (e.g.
``examples.pet_monitoring``). The examples import :mod:`convilyn_edge`; nothing in
``convilyn_edge`` imports them — an example / Solution Pack is removable.
"""
